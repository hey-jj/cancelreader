[package]
name = "cancelreader"
version = "0.1.0"
edition = "2021"
description = "cancelable blocking stdin/raw-input reader for aborting TUI reads on shutdown"
license = "MIT"
repository = "https://github.com/hey-jj/cancelreader"
readme = "README.md"

[dependencies]
